# ini_parser
一个简单的ini文件解析练习

The demo is written in c language, So please configure your C environment before using it.

We define a data structure called dictionary in the form of linked list to get data faster.

Some reading & writing related functions are defined in "ini.c".

And a test file called "config.ini" can  be  used  by "main.c".

